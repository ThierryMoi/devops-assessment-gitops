apiVersion: v1
kind: Service
metadata:
  name: {{ include "assessment-app.fullname" . }}
  labels:
    {{- include "assessment-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "assessment-app.selectorLabels" . | nindent 4 }}
