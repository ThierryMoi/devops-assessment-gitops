{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace.name }}
  labels:
    {{- include "assessment-app.labels" . | nindent 4 }}
    environment: prod
{{- end }}
